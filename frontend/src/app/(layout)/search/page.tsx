import SearchClientPage from "@/components/client/searchClient";

export const metadata = {
  title: "Search",
  description: "Page for search",
};

const SearchPage = () => {
  return <SearchClientPage />;
};

export default SearchPage;
