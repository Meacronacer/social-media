import SettingsClientPage from "@/components/client/settingsClient";

export const metadata = {
  title: "Settings",
  description: "Page for settings",
};

const SettingsPage = () => {
  return <SettingsClientPage />;
};

export default SettingsPage;
