import ChatClient from "@/components/client/chatClient";

export const metadata = {
  title: "Chats",
  description: "Page for chats",
};

const ChatsPage = () => {
  return <ChatClient />;
};

export default ChatsPage;
