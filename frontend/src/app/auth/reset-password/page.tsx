import ResetPasswordForm from "@/components/forms/resetPasswordForm";

export const metadata = {
  title: "Reset password",
  description: "Page for updated password",
};

const SearchPage = () => {
  return <ResetPasswordForm />;
};

export default SearchPage;
